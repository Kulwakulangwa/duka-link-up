{
  "buildCommand": "bun run build && bun build dist/server/index.mjs --outfile api/index.js --target node --bundle",
  "outputDirectory": "dist/client",
  "installCommand": "bun install",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index"
    }
  ]
}
